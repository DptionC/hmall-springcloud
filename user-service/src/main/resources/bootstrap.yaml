spring:
  application:
    name: user-service #微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.128:8848  #nacos地址
      config:
        file-extension: yaml #文件名后缀
        shared-configs: #共享配置
          - dataId: shared-jdbc.yaml #共享mybatis配置
          - dataId: shared-log.yaml #共享日志配置
          - dataId: shared-swagger.yaml #共享接口文档配置
          - dataId: shared-sentinel.yaml #共享接口文档配置